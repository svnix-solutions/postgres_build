# Postgres + pgAdmin for Node3.
#
# PGDATA lives on a local Docker volume, NOT on the NAS. Postgres needs
# reliable fsync and file locking, which NFS does not dependably provide.
# The NAS is bind-mounted read-write at /backup purely for dumps.
#
# POSTGRES_IMAGE swaps the whole server, e.g. to a PostGIS build. Two rules if
# you change it on a cluster that already has data:
#
#   1. Keep the SAME libc. Collation ordering comes from the C library, so
#      moving between a musl (alpine) and a glibc (debian) image under an
#      existing PGDATA can silently corrupt every text btree index. Postgis
#      publishes an -alpine variant precisely so this swap is a no-op:
#      postgis/postgis:17-3.5-alpine is postgres:17-alpine plus PostGIS.
#   2. Keep the SAME major version, and do not go backwards on the minor.
#
# The backup job deliberately uses the same image so pg_dumpall always matches
# the server it is dumping.

services:
  postgres:
    image: ${POSTGRES_IMAGE:-postgres:17-alpine}
    container_name: postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      # Keep the cluster in a subdirectory so the volume root (lost+found etc.)
      # never confuses initdb.
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - pgdata:/var/lib/postgresql/data
      - type: bind
        source: ${PGBACKUP_PATH:?PGBACKUP_PATH is required}
        target: /backup
        bind:
          # Fail loudly if the NAS is not mounted. Without this Docker would
          # silently create an empty local directory and "backups" would go to
          # the node's own disk.
          create_host_path: false
    # Bound to the node's LAN address, not 0.0.0.0.
    ports:
      - "${POSTGRES_BIND:-127.0.0.1}:5432:5432"
    shm_size: 256mb
    stop_grace_period: 1m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - db

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:?PGADMIN_EMAIL is required}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:?PGADMIN_PASSWORD is required}
      PGADMIN_CONFIG_SERVER_MODE: "True"
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
      PGADMIN_DISABLE_POSTFIX: "True"
    volumes:
      - pgadmin:/var/lib/pgadmin
    ports:
      - "${PGADMIN_BIND:-127.0.0.1}:5050:80"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - db

  # One-off backup job. "profiles" keeps it out of normal deploys -- it only
  # runs when invoked, via Komodo's RunStackService (see the "Postgres Backup"
  # procedure in resources/procedures/).
  backup:
    image: ${POSTGRES_IMAGE:-postgres:17-alpine}
    profiles: ["backup"]
    restart: "no"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGHOST: postgres
      PGUSER: ${POSTGRES_USER:-postgres}
      PGPASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-14}
    volumes:
      - ./backup.sh:/usr/local/bin/backup.sh:ro
      - type: bind
        source: ${PGBACKUP_PATH:?PGBACKUP_PATH is required}
        target: /backup
        bind:
          create_host_path: false
    entrypoint: ["/bin/sh", "/usr/local/bin/backup.sh"]
    networks:
      - db

volumes:
  pgdata:
  pgadmin:

networks:
  db:
    name: ${DB_NETWORK:-db}
